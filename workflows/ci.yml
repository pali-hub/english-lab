name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  setup-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.10]
        node-version: [18]

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Instalar Poetry
        run: pip install poetry

      - name: Instalar dependências Python
        working-directory: libs/parser
        run: poetry install

      - name: Rodar testes Python
        working-directory: libs/parser
        run: poetry run pytest

      - name: Configurar Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Instalar dependências do frontend
        working-directory: apps/web
        run: npm ci

      - name: Rodar lint do frontend
        working-directory: apps/web
        run: npm run lint

      - name: Rodar testes do frontend
        working-directory: apps/web
        run: npm run test -- --ci --coverage

      - name: Build do frontend
        working-directory: apps/web
        run: npm run build

      # Adicione steps para outros apps/libs conforme necessário

      # Exemplo: Upload de cobertura de testes (opcional)
      # - name: Upload coverage
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: coverage-report
      #     path: apps/web/coverage/
